# Task Discussion Summary

- The user asked for a TypeScript library boilerplate intended for future npm publishing.
- The selected package format is dual ESM and CommonJS output with declaration files.
- The selected release workflow is GitHub Actions publishing through npm Trusted Publishing with OIDC.
- The package targets Node.js `>=20`.
- The user required English-only documentation, exact dependency versions, per-task branches, verified commits, and
  task/decision logging.
